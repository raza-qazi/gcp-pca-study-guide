// ─── ASYNC & MESSAGING ────────────────────────────────────────────────────────

const ASYNC_MESSAGING = [

  {
    id: 'eventarc', name: 'Eventarc', family: 'Async & Messaging',
    tag: 'Event Routing & Triggers', launched: '2021',
    tldr: 'Routes events from 130+ GCP services and custom sources to Cloud Run, GKE, Workflows, or any HTTP endpoint — using CloudEvents 1.0.',
    description: 'Eventarc is GCPʼs event routing backbone. It standardises how events from Google Cloud services reach your compute targets. There are three trigger sources: direct events (Cloud Storage, BigQuery, Cloud Run, Pub/Sub), Audit Log-based events (any GCP API call — over 130 services including Cloud SQL, Spanner, and IAM), and Pub/Sub topics for custom event sources. All events are delivered in CloudEvents 1.0 format. Targets include Cloud Run services, Cloud Run Functions (Gen 2), GKE services, and Workflows. Eventarc handles delivery retries, dead-lettering (via Pub/Sub), and event filtering so your services only receive the events they care about.',
    strengths: [
      'Audit Log triggers: ANY GCP API call becomes an event (130+ services)',
      'CloudEvents 1.0 standard — portable event schema',
      'Direct triggers for Cloud Storage, Pub/Sub, BigQuery, Firestore',
      'Fan-out: one event → multiple triggers / targets',
      'Built-in retry and dead-letter via Pub/Sub',
      'Tight integration with Workflows for multi-step event processing',
      'No infrastructure to manage — fully serverless',
    ],
    weaknesses: [
      'Audit Log triggers have ~60s delivery latency (not real-time)',
      'Cannot trigger VMs or Cloud Functions Gen 1 directly',
      'Event filtering limited to attribute matching (no payload-based routing)',
      'Audit Log volume can be high — filter carefully to avoid noisy triggers',
    ],
    useCases: [
      'Trigger Cloud Run on Cloud Storage object upload',
      'Fire Workflows on BigQuery job completion',
      'React to any GCP API call (IAM policy change, Cloud SQL update)',
      'Custom event bus: publish to Pub/Sub → Eventarc fans out to multiple Cloud Run services',
      'Audit-driven automation: detect misconfigured resource → trigger remediation function',
      'Cross-service sagas: Eventarc chains Pub/Sub → Cloud Run → Workflows',
    ],
    bestFor: 'Event-driven architectures where multiple GCP services need to trigger downstream Cloud Run, Workflows, or GKE processing without point-to-point wiring',
    notFor: 'Sub-second real-time event streaming (use Pub/Sub + Dataflow); complex event transformation (use Dataflow); direct VM or Gen 1 Cloud Functions triggers',
    latency: 'Direct: milliseconds; Audit Log: ~60s', scale: 'Millions of events/day per trigger', pricing: 'Per million events routed (free tier: 2M/month)',
    sla: '99.95%', consistency: 'At-least-once delivery', dataModel: 'CloudEvents 1.0 envelope → target HTTP POST',
    keyDiff: 'Eventarc = event router (source → target wiring). Pub/Sub = raw message bus (no schema, no routing logic). Cloud Tasks = explicit task queue (one task, one handler). Eventarc often sits on top of Pub/Sub internally but adds source connectors, CloudEvents formatting, and multi-target routing.',
    examTip: '"Trigger Cloud Run when file uploaded to GCS" or "react to any GCP API call" → Eventarc. Audit Log triggers = most powerful: any GCP action (IAM change, SQL update, etc.) can fire your function. CloudEvents 1.0 = the standard event envelope Eventarc uses.',
    integrations: ['Cloud Run', 'Cloud Run Functions', 'Workflows', 'Cloud Pub/Sub', 'Cloud Storage', 'BigQuery', 'Firestore', 'GKE'],
    debugTips: [
      'Trigger not firing: check the Eventarc trigger status in Cloud Console → confirm the service account has eventarc.eventReceiver role on the target.',
      'Audit Log trigger missing events: verify the audit log type is enabled (DATA_READ, DATA_WRITE, ADMIN_READ) for the service in IAM → Audit Logs.',
      'Cloud Run target returning 4xx/5xx: Eventarc retries with exponential backoff; view retry attempts in Cloud Logging → filter resource.type="eventarc.googleapis.com/Trigger".',
      'Event payload schema: print the entire CloudEvent in your handler for debugging — the HTTP request body contains the JSON CloudEvent envelope.',
      'Dead-lettering: configure a Pub/Sub dead-letter topic on the underlying subscription for events that exhaust retries.',
    ],
  },

  {
    id: 'workflows', name: 'Workflows', family: 'Async & Messaging',
    tag: 'Serverless Orchestration', launched: '2020',
    tldr: 'Serverless workflow orchestration — chain HTTP calls, GCP APIs, and Cloud Run services with retries, parallel steps, and persistent execution state.',
    description: 'Workflows is a fully managed serverless orchestration service for sequencing HTTP-based services and GCP APIs. You define steps in YAML or JSON: HTTP call steps, parallel branches, conditional logic, retry policies, error handling (try/catch), and sub-workflows. Each execution persists state durably — a workflow can wait days for a callback (using callbacks and Eventarc integration) without consuming resources. Integration Connectors provide pre-built steps to 100+ SaaS and GCP services (BigQuery, Cloud SQL, Salesforce, Sheets). Workflows replaces complex state management in application code or Step Functions-style orchestration patterns on GCP.',
    strengths: [
      'Durable execution state — survives restarts, waits days without cost',
      'Parallel steps: fan-out concurrent API calls, collect results',
      'Callbacks: pause and wait for an external HTTP callback (human approval, async job)',
      'Integration Connectors: 100+ pre-built connectors (Salesforce, BigQuery, SAP, etc.)',
      'Built-in retry with exponential backoff + jitter',
      'No servers — scales to zero, pay per step execution',
      'Eventarc integration: trigger Workflows on any GCP event',
    ],
    weaknesses: [
      'YAML/JSON workflow syntax has a learning curve',
      'Not for real-time streaming (use Dataflow); not a message bus',
      'Step limits: 32,768 steps max per execution',
      'Long-running workflows with many steps accumulate cost',
      'No native UI for step visualisation (use Cloud Monitoring for execution history)',
    ],
    useCases: [
      'Multi-step data pipeline: trigger BigQuery → Cloud Run transform → write to GCS',
      'Human-in-the-loop: pause workflow, send approval email, resume on callback',
      'Microservice saga: orchestrate multi-service transaction with compensating steps on failure',
      'Batch job orchestration: fan-out parallel Cloud Run Jobs, await all results',
      'SaaS integration: poll Salesforce, enrich data in Cloud SQL, publish to Pub/Sub',
      'Image processing pipeline: GCS upload → Eventarc → Workflows → Vision AI → store results',
    ],
    bestFor: 'Replacing application-level orchestration code with a durable, serverless state machine — especially for multi-step GCP API chains, parallel fan-out, or human approval flows',
    notFor: 'Stream processing (Dataflow); high-frequency event handling per-message (Cloud Run + Pub/Sub); real-time latency-sensitive pipelines',
    latency: 'Step execution: ~100ms overhead per step', scale: 'Unlimited concurrent executions', pricing: 'Per step execution (5,000 free steps/month)',
    sla: '99.95%', consistency: 'Exactly-once step execution (durable state)', dataModel: 'Steps (HTTP call, assign, condition, parallel, return) + persistent execution state',
    keyDiff: 'Workflows = orchestration (the workflow calls services). Pub/Sub = choreography (services react to events without central coordinator). Eventarc = event routing (fires Workflows as a target). Cloud Tasks = task queue (one-off deferred execution). Use Workflows when you need a visible, auditable, retryable multi-step process.',
    examTip: '"Chain multiple API calls", "pause for human approval", "orchestrate multi-step GCP pipeline", "fan-out parallel calls and collect results" → Workflows. Workflows + Eventarc = event-driven durable orchestration. Callbacks = wait for async completion without polling.',
    integrations: ['Eventarc', 'Cloud Run', 'Cloud Run Functions', 'BigQuery', 'Cloud SQL', 'Cloud Tasks', 'Secret Manager', 'Pub/Sub'],
    debugTips: [
      'Execution stuck: check the execution detail in Cloud Console → Workflows → Executions for the step where it is waiting or failed.',
      'HTTP step 403: the Workflows service account must have permission to call the target URL — grant the SA the appropriate role on the target service.',
      'Callback not received: verify the callback endpoint URL is correct and that the external system is calling it with the expected HTTP method and body.',
      'Parallel step timeout: each parallel branch has an independent timeout; if one branch exceeds the deadline the entire parallel step fails — add per-branch try/except.',
      'Retry loops not stopping: check the retry predicate — by default only 5xx errors retry; add specific HTTP status codes if needed.',
    ],
  },

]

// ─── PLATFORM ─────────────────────────────────────────────────────────────────

const PLATFORM = [

  {
    id: 'app-engine', name: 'App Engine', family: 'Platform',
    tag: 'Managed PaaS (Standard & Flexible)', launched: '2008',
    tldr: 'GCPʼs original PaaS — deploy code, not containers. Standard environment scales to zero in milliseconds; Flexible uses Docker on Compute Engine.',
    description: 'App Engine is Googleʼs fully managed platform-as-a-service (PaaS) and the first GCP product (launched 2008). Two environments: Standard (sandboxed language runtimes — Python, Java, Go, Node.js, PHP, Ruby; scales to zero in ~milliseconds; free tier; no Docker required) and Flexible (any Docker container on Compute Engine VMs; no scale-to-zero; minimum 1 instance; supports any language). App Engine manages load balancing, auto-scaling, health checks, and TLS. Traffic splitting enables A/B testing and canary releases across versions. Integrated with Cloud Monitoring, Cloud Logging, Error Reporting, and Cloud Trace. For most new workloads, Cloud Run supersedes App Engine Flexible; Standard remains relevant for legacy apps and the free tier.',
    strengths: [
      'Standard: scales to zero with ~millisecond cold starts (lightest FaaS-adjacent model)',
      'Standard: generous free tier (28 instance-hours/day)',
      'Built-in traffic splitting for canary/blue-green deployments',
      'Fully managed TLS, load balancing, auto-scaling',
      'Integrated Cloud Monitoring, Logging, Trace, Error Reporting',
      'Standard: memcache and task queue APIs (legacy but stable)',
      'Cron jobs via cron.yaml — no external scheduler needed',
    ],
    weaknesses: [
      'Standard: sandboxed runtime — limited OS access, no arbitrary binaries',
      'Flexible: minimum 1 instance (no scale-to-zero) — idle costs',
      'Flexible: slower deploys than Cloud Run (VM-based)',
      'Single-region per app (no regional/global distribution within App Engine itself)',
      'Cloud Run is preferred for new containerised workloads',
      'Standard runtimes lag latest language versions',
    ],
    useCases: [
      'Legacy Python/Java web apps migrated from on-prem',
      'Internal tools needing simple deploy + TLS without Kubernetes',
      'Cron-driven batch tasks via cron.yaml',
      'Traffic splitting for canary releases without extra infrastructure',
      'Low-traffic APIs where scale-to-zero free tier matters (Standard)',
      'Flexible: apps with native library dependencies (e.g. C extensions, ffmpeg)',
    ],
    bestFor: 'Teams migrating legacy PaaS workloads to GCP, or applications that benefit from the Standard free tier and zero-config scaling without needing containers',
    notFor: 'New containerised workloads (use Cloud Run); stateful workloads (use GKE); high-performance GPU/ML inference; multi-region deployments',
    latency: 'Standard: ~50ms cold start; Flexible: ~60–120s deploy', scale: 'Standard: 0 to thousands of instances; Flexible: min 1', pricing: 'Standard: per instance-hour (free tier); Flexible: per vCPU/mem-hour (no free tier)',
    sla: '99.95%', consistency: 'N/A', dataModel: 'Code deployed as versions; traffic split across versions via weights',
    keyDiff: 'App Engine Standard = lightest PaaS, scale to zero, free tier, sandboxed runtimes. App Engine Flexible = Docker on VMs, no scale-to-zero, any language. Cloud Run = Docker, scale to zero, regional, preferred for new apps. GKE = full Kubernetes control.',
    examTip: '"Scale to zero for a web app without containers" or "free tier PaaS" → App Engine Standard. "Legacy App Engine migration" → keep in App Engine or modernise to Cloud Run. Standard vs Flexible: Standard = sandboxed + scale to zero; Flexible = Docker + VM + no free scale-to-zero.',
    integrations: ['Cloud SQL', 'Cloud Datastore / Firestore', 'Cloud Memorystore', 'Cloud Tasks', 'Cloud Scheduler', 'Artifact Registry', 'Cloud Monitoring'],
    debugTips: [
      'App not receiving traffic: check version traffic allocation in App Engine → Versions — only versions with traffic share > 0 receive requests.',
      'Standard sandbox error: operations not allowed in sandbox (file writes outside /tmp, sockets to non-approved services) — migrate to Flexible or Cloud Run.',
      'Flexible deployment stuck: check Compute Engine → Instance groups for the App Engine Flexible managed instance group — look for unhealthy instances.',
      'Cron jobs not firing: validate cron.yaml syntax (schedule in UTC) and check App Engine → Task queues → Cron jobs in Cloud Console.',
      'High latency after idle: Standard cold starts add latency — set min_instances: 1 in app.yaml for latency-sensitive endpoints.',
    ],
  },

  {
    id: 'firebase', name: 'Firebase', family: 'Platform',
    tag: 'Mobile & Web App Backend Platform', launched: '2011',
    tldr: 'Full mobile/web backend platform — Firestore, Auth, Hosting, Cloud Messaging, Remote Config, and serverless functions all under one SDK.',
    description: 'Firebase is Googleʼs mobile and web application development platform, fully integrated with GCP. Core services: Firestore (document database with real-time sync and offline support — the primary Firebase database since 2017), Firebase Authentication (OAuth 2.0, email/password, phone, anonymous — 50+ providers), Realtime Database (legacy JSON tree, low latency, regional), Cloud Storage for Firebase (binary storage backed by GCS), Firebase Hosting (CDN-backed static + dynamic hosting with Cloud Run integration), Cloud Functions for Firebase (Cloud Run Functions with Firebase SDK shortcuts), Firebase Cloud Messaging (FCM — push notifications, 500B+ messages/day globally), and Remote Config (server-side feature flags and A/B testing without app updates). Firebase integrates natively with Google Analytics, Crashlytics, App Distribution, and Vertex AI for mobile ML.',
    strengths: [
      'Realtime Firestore sync — offline-first with client SDK for web/iOS/Android',
      'Firebase Auth: 50+ OAuth providers + anonymous + phone auth out of the box',
      'FCM: free push notifications at 500B+/day global scale',
      'Remote Config: server-side feature flags without app store updates',
      'Hosting: CDN-backed, custom domains, preview channels per PR',
      'Firestore security rules: row-level auth enforced at the database layer',
      'App Check: protect backend resources from abuse/bots',
    ],
    weaknesses: [
      'Firestore: no complex joins or aggregations (limited query expressiveness)',
      'Vendor lock-in: Firebase SDKs and security rules are Firebase-specific',
      'Realtime Database: legacy, limited query capability, single JSON tree',
      'Scaling costs: Firestore reads priced per document — N+1 query patterns are expensive',
      'Cloud Functions for Firebase cold starts (Gen 1); use Gen 2 (Cloud Run Functions) for production',
    ],
    useCases: [
      'Mobile apps needing offline sync and realtime data (chat, collaborative docs)',
      'Web apps with social login (Google, Apple, GitHub OAuth)',
      'Push notification campaigns via Firebase Cloud Messaging',
      'A/B testing UI changes without app release via Remote Config',
      'Static + serverless hybrid hosting (Hosting + Cloud Run backend)',
      'MVP / hackathon: full backend with Auth + DB + Storage + Functions in hours',
    ],
    bestFor: 'Mobile and web applications needing real-time data sync, offline support, and authentication without managing backend infrastructure — especially teams building consumer apps fast',
    notFor: 'Complex relational data (use Cloud SQL); analytical workloads (use BigQuery); stateful compute (use GKE); existing enterprise backend migration (Firebase SDKs add coupling)',
    latency: 'Firestore realtime: ~100–300ms; FCM: seconds globally', scale: 'Firestore: 1M concurrent connections; FCM: 500B+ messages/day', pricing: 'Spark (free tier) / Blaze (pay-as-you-go); Firestore per read/write/storage',
    sla: '99.95% (Firestore)', consistency: 'Firestore: strong consistency (single-document); eventual (cross-shard queries)', dataModel: 'Firestore: document → collection hierarchy; Realtime DB: JSON tree',
    keyDiff: 'Firebase Firestore vs Cloud Firestore: they are the same product — Firestore runs in both Firebase and GCP consoles. Firebase = the mobile/web SDK layer on top of GCP services. Firestore vs Realtime Database: Firestore is strongly consistent, richer queries, scales better; Realtime Database is legacy, lower latency for simple use cases.',
    examTip: '"Mobile app with offline sync and realtime updates" → Firebase Firestore. "Push notifications to mobile apps" → Firebase Cloud Messaging (FCM). "OAuth login with Google/Apple" → Firebase Authentication. Firestore = the same as Cloud Firestore — use Firebase console for mobile, GCP console for server-side.',
    integrations: ['Cloud Run', 'Cloud Run Functions', 'Google Analytics', 'BigQuery', 'Cloud Storage', 'Vertex AI', 'Cloud Monitoring', 'App Check'],
    debugTips: [
      'Firestore permission denied: check security rules in Firebase Console → Firestore → Rules — use the Rules Playground to simulate a read/write with test auth tokens.',
      'FCM messages not delivered: check FCM registration tokens are up to date — tokens expire after 270 days of inactivity; handle token refresh in the client SDK.',
      'Offline sync conflict: Firestore last-write-wins by default; use transactions or optimistic locking (FieldValue.serverTimestamp comparison) for conflict resolution.',
      'High Firestore read costs: use query cursors + pagination, avoid listening to entire collections; index composite queries to avoid full scans.',
      'Cold starts on Cloud Functions for Firebase Gen 1: migrate to Gen 2 (Cloud Run Functions) for concurrency support and min-instances configuration.',
    ],
  },

  {
    id: 'looker', name: 'Looker', family: 'Platform',
    tag: 'Enterprise BI & Embedded Analytics', launched: '2012',
    tldr: 'Enterprise BI platform built on LookML — a semantic data model layer that governs metrics definitions and enables embedded analytics in any application.',
    description: 'Looker (acquired by Google 2019, integrated into Google Cloud) is an enterprise business intelligence and embedded analytics platform. Its core differentiator is LookML — a SQL-generating modeling language that defines metrics, dimensions, and joins as reusable semantic layer on top of your data warehouse. All queries are generated from LookML, ensuring consistent metrics across dashboards, embedded charts, and the Looker API. Looker connects directly to BigQuery, AlloyDB, Cloud SQL, Snowflake, Redshift, and 50+ other databases via JDBC drivers. Looker Studio (formerly Data Studio) is the free, simpler dashboarding tool; Looker is the enterprise product with governance, API access, embedding, and version-controlled LookML. Looker Blocks provide pre-built LookML models for BigQuery ML, Google Ads, Firebase Analytics, and more.',
    strengths: [
      'LookML semantic layer: single source of truth for metrics — no metric drift across teams',
      'Embedded analytics: iFrame or API embedding in any product (white-label)',
      'Direct database queries — no data extract/copy; always fresh',
      'Looker API: programmatic query execution, dashboard delivery, data actions',
      'Git-backed LookML: version control, CI/CD, peer review for data models',
      'Looker Blocks: pre-built models for BigQuery, Google Ads, Firebase, etc.',
      'Scheduled deliveries: dashboards to Slack, email, GCS, webhooks',
    ],
    weaknesses: [
      'LookML has a significant learning curve for data teams',
      'Expensive enterprise licensing (not self-serve for small teams)',
      'Looker-hosted only (no self-hosted option since Google acquisition)',
      'Not real-time: query latency depends on underlying warehouse',
      'Looker Studio (free) vs Looker (paid) confusion — very different products',
    ],
    useCases: [
      'Company-wide KPI dashboards with governed, consistent metric definitions',
      'Embedded analytics in a SaaS product (white-label charts for customers)',
      'Self-serve data exploration for business users without SQL knowledge',
      'Automated report delivery: daily metrics to Slack channels or email',
      'BigQuery data modeling: LookML on top of BigQuery for data mesh',
      'Data action workflows: trigger Salesforce update or Pub/Sub message from a Looker dashboard',
    ],
    bestFor: 'Enterprises needing consistent, governed metric definitions across multiple teams and products, or SaaS companies wanting to embed analytics directly in their application',
    notFor: 'Ad-hoc individual analysis (use Looker Studio or Colab); small teams on a budget (use Looker Studio free tier); real-time streaming dashboards (use Grafana + Prometheus)',
    latency: 'Query latency = underlying warehouse speed', scale: 'Depends on connected warehouse (BigQuery: petabyte-scale)', pricing: 'Enterprise licensing (per user / deployment tier)',
    sla: '99.9%', consistency: 'N/A (queries underlying DB directly)', dataModel: 'LookML: Explores (join sets), Views (tables), Dimensions (columns), Measures (aggregations)',
    keyDiff: 'Looker = semantic layer (LookML governs metrics) + enterprise BI + embedding. Looker Studio = free, simple drag-and-drop dashboards (no LookML). BigQuery BI Engine = in-memory acceleration layer for BigQuery queries, not a BI tool. Looker sits on top of BigQuery, not instead of it.',
    examTip: '"Consistent metric definitions across teams" or "embedded analytics in a product" → Looker. "Free, simple GCP dashboard" → Looker Studio. LookML = SQL-generating semantic model — the reason Looker exists. Looker is not a data warehouse; it queries one.',
    integrations: ['BigQuery', 'AlloyDB', 'Cloud SQL', 'Pub/Sub', 'Cloud Storage', 'Vertex AI', 'Google Workspace', 'Salesforce'],
    debugTips: [
      'LookML compile errors: run "Validate LookML" in the Looker IDE — it catches syntax errors, broken join references, and undefined fields before deployment.',
      'Slow dashboard: identify slow tiles by hovering each tile → "Explore from here" → check the generated SQL in the Explore → run EXPLAIN in BigQuery to find missing indexes or full scans.',
      'Embedding 401/403: verify the signed embed URL has the correct group_ids, external_user_id, and that the embedded user has access to the relevant LookML model.',
      'Metric inconsistency: check for duplicate measure definitions across LookML views — enforce a single canonical view per business entity.',
      'Scheduled delivery failing: check the delivery schedule in Admin → Scheduled Plans; delivery errors appear here with HTTP status codes for webhook destinations.',
    ],
  },

  {
    id: 'cloud-endpoints', name: 'Cloud Endpoints', family: 'Platform',
    tag: 'Lightweight API Gateway (OpenAPI / gRPC)', launched: '2016',
    tldr: 'Lightweight API proxy for Cloud Run, GKE, App Engine, and Compute Engine — API key auth, JWT validation, quota, and monitoring with zero infrastructure.',
    description: 'Cloud Endpoints is Googleʼs lightweight API management solution for exposing and securing APIs running on GCP. It sits as an Extensible Service Proxy (ESP or ESPv2) — a containerised Envoy-based sidecar or DaemonSet that runs alongside your backend. Endpoints supports three API styles: OpenAPI (REST APIs defined via swagger.yaml), gRPC (with protobuf service definitions and HTTP/JSON transcoding), and gRPC-Web. Features: API key authentication, JWT validation (Firebase Auth, Google ID tokens, Auth0), rate limiting and per-consumer quotas, request/response logging in Cloud Logging, and distributed tracing in Cloud Trace. The entire API surface is defined in a swagger or proto config file and deployed via gcloud — no additional portal or infrastructure. For full enterprise API management (developer portal, monetisation, analytics), use Apigee instead.',
    strengths: [
      'Zero additional infrastructure — ESP runs as a sidecar alongside your backend',
      'gRPC + HTTP/JSON transcoding: serve REST and gRPC from the same service',
      'Firebase Auth and Google ID token validation out of the box',
      'API key issuance and per-key quota enforcement',
      'Cloud Logging + Cloud Trace integration — every request logged and traced',
      'OpenAPI spec as the source of truth (swagger.yaml → deployed API surface)',
      'Works with Cloud Run, GKE, App Engine, Compute Engine',
    ],
    weaknesses: [
      'No developer portal (use Apigee for self-service developer onboarding)',
      'No monetisation or billing plans (Apigee)',
      'Limited traffic management (no canary, circuit breaker, retries at gateway level)',
      'ESPv2 requires service account with servicemanagement.services.report permission',
      'Complex gRPC config when combining HTTP transcoding with streaming RPCs',
    ],
    useCases: [
      'Secure a Cloud Run API with JWT authentication (Firebase/Google tokens)',
      'Expose a gRPC service as a REST API via HTTP/JSON transcoding',
      'Rate-limit third-party API consumers via API keys and quotas',
      'Add distributed tracing to an existing Cloud Run service without code changes',
      'Internal microservice gateway: enforce service-to-service auth via JWT',
      'Lightweight API gateway for App Engine standard services',
    ],
    bestFor: 'Teams who need API key management, JWT auth, and quota enforcement on Cloud Run/GKE/App Engine backends without the cost or complexity of Apigee',
    notFor: 'Enterprise developer portals (Apigee); API monetisation (Apigee); advanced traffic management like canary or circuit breaker (use Istio/ASM); non-GCP backends',
    latency: '~2–5ms proxy overhead (ESP/ESPv2)', scale: 'Inherits backend scale; ESP is stateless and horizontally scalable', pricing: 'Free (Cloud Endpoints itself is free; pay for backend compute + Cloud Logging/Trace volume)',
    sla: 'N/A (proxy runs as part of your service)', consistency: 'N/A', dataModel: 'OpenAPI (swagger.yaml) or protobuf (.proto) service definition → deployed to Service Management API',
    keyDiff: 'Cloud Endpoints = lightweight proxy (JWT, API keys, quotas) — no portal, no monetisation. Apigee = full enterprise API management (portal, analytics, monetisation, developer ecosystem). Cloud Load Balancing = L7 routing without API management semantics. API Gateway (GCP) = serverless, regional, newer alternative to Cloud Endpoints for Cloud Run/Functions backends.',
    examTip: '"Add API key auth to a Cloud Run service" or "expose gRPC as REST" → Cloud Endpoints (ESPv2). "Enterprise developer portal + monetisation" → Apigee. "Simple JWT validation without infrastructure" → Cloud Endpoints is the lightest option. ESPv2 = Envoy-based; ESP = nginx-based (legacy).',
    integrations: ['Cloud Run', 'GKE', 'App Engine', 'Compute Engine', 'Firebase Auth', 'Cloud IAM', 'Cloud Logging', 'Cloud Trace', 'Apigee'],
    debugTips: [
      'ESP startup failing: check that the service account has roles/servicemanagement.serviceController — this is required for ESP to check API configuration.',
      'API key rejected (401): verify the API key is enabled for the correct service name (your Endpoints service URL) in APIs & Services → Credentials.',
      'JWT validation failing: confirm the issuer and audiences in the swagger.yaml securityDefinitions match the token claims exactly — use jwt.io to decode and inspect the token.',
      'gRPC transcoding returning 404: check the HTTP annotation in the .proto file matches the path in the request — path parameters must match proto field names.',
      'Requests not appearing in Cloud Logging: verify ESP is running alongside the backend and that the service config has been successfully deployed (gcloud endpoints services deploy).',
    ],
  },

]

export const SERVICES_EXTRA = [...ASYNC_MESSAGING, ...PLATFORM]
